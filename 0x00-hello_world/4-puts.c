#include <stdio.h>

/**
 * main - This is the entry point
 *
 * Return to always check if its 0 which means success
 *
 */

int main(void)
{
	puts("\"Programming is like building a multilingual puzzle");
	return (0);
}
